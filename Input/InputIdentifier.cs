using System;
using System.Diagnostics.CodeAnalysis;

namespace MainGame.Input {
	//public struct InputIdentifier : IEquatable<InputIdentifier> {
	//	private readonly InputSource _sender;
	//	private readonly ValueType _inputCode;

	//	public InputIdentifier(InputSource source, ValueType code) {
	//		_sender = source;
	//		_inputCode = code;
	//	}

	//	public bool Equals([AllowNull] InputIdentifier other)
	//		=> other._sender == _sender && _inputCode == other._inputCode;
	//	public override bool Equals(object obj)
	//		=> obj is InputIdentifier identifier && identifier._inputCode == _inputCode && _inputCode 
	//}
}
